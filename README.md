# Creating a task application with SQLite database in Flutter.

The application utilizes SQLite for deletion, addition, and listing functions, in conjunction with the database.

I might add even more.
